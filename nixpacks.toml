[phases.setup]
nixPkgs = ["php81", "php81Packages.composer", "php81Extensions.zip", "php81Extensions.gd", "php81Extensions.mbstring", "php81Extensions.mysqli", "php81Extensions.pdo", "php81Extensions.pdo_mysql"]

[phases.install]
cmds = ["composer install --no-dev --optimize-autoloader"]

[start]
cmd = "php -S 0.0.0.0:$PORT -t tao/views"
